Plan of action:
k8s with no ingress and manual installation
k8s with container registry
k8s with auto installation

postgres connectivity
redis connectivity
gitlab enterprise configuration

k8s with ingress and cert

helm repo add gitlab https://charts.gitlab.io/
helm repo update

helm upgrade --install gitlab gitlab/gitlab --timeout 600s --debug --dry-run --values gitlab_values.yaml

helm upgrade --install gitlab gitlab/gitlab --timeout 600s --values gitlab_values.yaml
