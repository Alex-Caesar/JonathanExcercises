helm repo add gitlab https://charts.gitlab.io/
helm repo update

helm upgrade --install gitlab gitlab/gitlab --timeout 600s --debug --dry-run --values gitlab_default.yaml -o yaml > testingpostgres.yaml

helm upgrade --install gitlab gitlab/gitlab --timeout 600s --values gitlab_default.yaml --wait