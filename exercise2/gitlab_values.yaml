postgresql:
  install: false
redis:
  install: false
certmanager-issuer:
  email: nil

global:
  hosts:
    domain: exercise2.alex.com
    externalIP: nil
  edition: ee
  ingress:
    configureCertManager: false
  redis:
    host: *.redis.cache.windows.net
    auth:
      enabled: false
  psql:
    host: nil.postgres.database.azure.com
    username: psqladmin
    password:
      secret: psql-pword
      key: password
  
gitlab:
  migrations:
    initialRootPassword:
      secret: gitlab-pword
      key: password